// ----------4:Repetir al usuario que ingrese un numero entero positivo e imprimir todos los numeros correlativos
// entre el ingresado por el usuario y uno menos el doble del mismo  
var i;
i = prompt("ingrese un numero");
var x = 0;
for(x=0; x<=(i*2)-(1); x++){
    console.log(x);
}
